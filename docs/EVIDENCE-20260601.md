# Evidence Archive: 2026-06-01

Last refreshed: 2026-06-01T05:36:42+08:00.

## Current Checkout Addendum

The current 2026-06-01 documentation alignment audit found that the mounted
`/home/x.go` checkout no longer matches the x.go integration evidence archived
below.

Current `/home/x.go` evidence:

- `/home/x.go/go.mod` does not require `github.com/bytechainx/postgresx`.
- With `/home/go.work` active, `go list -m github.com/bytechainx/postgresx`
  returns the sibling workspace module.
- With `GOWORK=off`, `go list -m github.com/bytechainx/postgresx` in
  `/home/x.go` reports that the module is not a known dependency.
- `/home/x.go/pkg/adapter/db/postgres/postgres.go` still imports
  `github.com/jackc/pgx/v5/pgxpool` directly.
- No `sqlc.yaml` or `sqlc.yml` file is present under `/home/x.go` at max depth
  3, and the collection-status files listed below are absent.

The x.go-specific successful commands below are therefore historical evidence,
not current verification claims for the mounted `/home/x.go` checkout.

## Historical Final Refresh Checks

- `make ci` passed in `/home/postgresx`.
- The focused local gate passed again:
  `gofmt -l .`, `go vet ./...`, `go test ./...`, `go test -race ./...`,
  and `./scripts/ci/secret_scan.sh`.
- `POSTGRES_TEST_DSN=<local temporary PostgreSQL DSN> go test -run 'Test(OpenPingStatsIntegration|MetricsTracingIntegration|WithinTxCommitRollbackIntegration|WithinTxRetryIntegration|MigratorUpDownUpIntegration)' -count=1 ./...`
  passed in `/home/postgresx` after the final migration dirty-state assertion
  was added.
- `POSTGRES_TEST_DSN=<local temporary PostgreSQL DSN> ./scripts/ci/migration_up_down_up.sh`
  passed again in `/home/postgresx` against a temporary local PostgreSQL
  database and role; the raw log records `version=1 dirty=false` after the
  first up and after the up/down/up cycle.
- The GitHub Actions workflow now provisions PostgreSQL 17, exports
  `POSTGRES_TEST_DSN`, and runs the live migration script instead of relying
  on a skip-only migration gate.
- `go run github.com/sqlc-dev/sqlc/cmd/sqlc@v1.31.1 generate` completed in
  `/home/x.go`.
- `go test -count=1 ./internal/market_data/server/state ./pkg/adapter/db/postgres ./internal/bootstrap`
  passed again in `/home/x.go`.
- `POSTGRES_TEST_DSN=<local temporary PostgreSQL DSN> go test -run TestCollectionStatusRepository -count=1 ./internal/market_data/server/state`
  passed in `/home/x.go` against a temporary local PostgreSQL database and
  role.
- `go test -count=1 ./cmd/gate/importboundary` passed in `/home/x.go`.
- `go run ./cmd/gate/importboundary --repo-root=.` passed in `/home/x.go`
  with `findings=0 errors=0 warnings=0`.
- `GOWORK=off go list -m` in `/home/postgresx` returned
  `github.com/bytechainx/postgresx`.
- `GOWORK=off go list -deps ./...` in `/home/postgresx` returned no
  `github.com/bytechainx/x.go` packages.
- `GOWORK=off go list -m github.com/bytechainx/postgresx` in the prior
  `/home/x.go` candidate returned
  `github.com/bytechainx/postgresx v0.1.0 => /home/postgresx`.
- `rg -n "pgxpool" internal/market_data/server/state` returned no matches in
  `/home/x.go`.
- `git diff --check` passed in both `/home/postgresx` and `/home/x.go`.
- `git tag --list v0.1.0` returned no tag in `/home/postgresx`.
- `timeout 30s git ls-remote --tags origin v0.1.0` returned no remote tag.
- `gh release view v0.1.0 --repo ZoneCNH/postgresx --json tagName,url,publishedAt`
  returned `release not found`.
- `docs/RELEASE_MANIFEST-v0.1.0.md` included the live
  `TestMetricsTracingIntegration` command in its local verification list.
- Raw command logs were archived under `docs/evidence/20260601/`.
  The module and dependency logs were generated with `GOWORK=off` so the
  surrounding `/home/go.work` workspace cannot add sibling-module entries.

## Requirement Audit

| ID | Status | Evidence |
| --- | --- | --- |
| REQ-001 | Satisfied locally | `GOWORK=off go list -m` returns `github.com/bytechainx/postgresx`. |
| REQ-002 | Satisfied locally | `GOWORK=off go list -deps ./...` has no `github.com/bytechainx/x.go` packages. |
| REQ-003 | Satisfied locally | `Open`, `OpenPool`, `Ping`, `Close`, and `Stats` are covered by unit and live integration tests. |
| REQ-004 | Satisfied locally | `WithinTx` commit, rollback, and retry paths pass unit and live integration tests. |
| REQ-005 | Satisfied locally | Migrator up/down/up and dirty-state assertions pass against local PostgreSQL. |
| REQ-006 | Satisfied locally | Root package exposes `DBTX`; postgresx examples use the sqlc-compatible contract. Current `/home/x.go` does not contain the archived collection-status integration. |
| REQ-007 | Satisfied locally | Error kind and PostgreSQL code classification tests pass in `go test ./...`. |
| REQ-008 | Satisfied locally | Health readiness/liveness tests pass, including live `Health` readiness. |
| REQ-009 | Satisfied locally | `TestMetricsTracingIntegration` exercises pluggable metrics and tracing adapters. |
| REQ-010 | Satisfied locally | Secret mask tests and `./scripts/ci/secret_scan.sh` pass. |
| REQ-011 | Satisfied locally | Live PostgreSQL integration tests pass with a temporary local database and role. |
| REQ-012 | Satisfied locally | `make ci` passes; live migration script passes with `POSTGRES_TEST_DSN`. Remote CI has not been observed. |
| REQ-013 | Satisfied with caveat | `docs/XGO_INTEGRATION.md` exists and now records the current `/home/x.go` non-integration status. |
| REQ-014 | Not satisfied in current `/home/x.go` checkout | Historical evidence below records a prior collection-status candidate, but the current checkout does not require `postgresx` or contain the listed collection-status files. |
| REQ-015 | Partial | Evidence, version pins, current x.go caveats, and migration state are documented here; tag and remote publication evidence are still missing. |

## Library Verification

Command: `make ci`

Result: passed.

Covered checks:

- `gofmt -w $(find . -name '*.go' -not -path './.git/*')`
- `go vet ./...`
- `go test ./...`
- `go test -race ./...`
- `./scripts/ci/secret_scan.sh`
- GitHub Actions provisions PostgreSQL 17, exports `POSTGRES_TEST_DSN`, and
  invokes `./scripts/ci/migration_up_down_up.sh`; the script remains skip-safe
  only for local runs without `POSTGRES_TEST_DSN`.

Observed package test results:

```text
ok   github.com/bytechainx/postgresx
ok   github.com/bytechainx/postgresx/internal/secretmask
```

Focused checks also passed:

```sh
gofmt -l .
go vet ./...
go test ./...
go test -race ./...
./scripts/ci/secret_scan.sh
```

`gofmt -l .` produced no output. `go list -deps ./... | rg
"github.com/bytechainx/x.go"` produced no output, confirming postgresx has no
reverse dependency on x.go. Searching for the x.go secret-directory prefix
outside `docs/goal.md` produced no output, confirming no hardcoded secret path
outside the captured goal text.

Raw command logs:

| Gate | Artifact |
| --- | --- |
| gofmt | `docs/evidence/20260601/gofmt.txt` |
| vet | `docs/evidence/20260601/go-vet.txt` |
| unit/package tests | `docs/evidence/20260601/go-test.txt` |
| race tests | `docs/evidence/20260601/go-test-race.txt` |
| secret scan | `docs/evidence/20260601/secret-scan.txt` |
| module list | `docs/evidence/20260601/dependencies.txt` |
| dependency packages without x.go | `docs/evidence/20260601/no-xgo-deps.txt` |

## Live Migration Gate

Environment: local PostgreSQL 17.9 with a temporary database and role created
only for this verification pass. The temporary database and role were removed
after the checks completed.

Command: `POSTGRES_TEST_DSN=<local temporary PostgreSQL DSN> ./scripts/ci/migration_up_down_up.sh`

Result: passed.

Raw command log: `docs/evidence/20260601/migration-up-down-up.txt`.

Observed package test results:

```text
    migrator_integration_test.go:43: migration after up: version=1 dirty=false
    migrator_integration_test.go:57: migration after up again: version=1 dirty=false
ok   github.com/bytechainx/postgresx
ok   github.com/bytechainx/postgresx/internal/secretmask [no tests to run]
```

Focused live integration command:

```sh
POSTGRES_TEST_DSN=<local temporary PostgreSQL DSN> go test -run 'Test(OpenPingStatsIntegration|MetricsTracingIntegration|WithinTxCommitRollbackIntegration|WithinTxRetryIntegration|MigratorUpDownUpIntegration)' -count=1 ./...
```

Result: passed.

Raw command log: `docs/evidence/20260601/postgres-integration.txt`.

```text
ok   github.com/bytechainx/postgresx
ok   github.com/bytechainx/postgresx/internal/secretmask [no tests to run]
```

## Historical x.go Integration Verification

This section records a prior local x.go integration candidate. It does not
describe the current `/home/x.go` checkout mounted during the documentation
alignment audit above.

The prior local `/home/x.go` checkout imported
`github.com/bytechainx/postgresx` via a local replace to `/home/postgresx`.

Commands:

```sh
go test ./internal/market_data/server/state ./pkg/adapter/db/postgres ./internal/bootstrap
POSTGRES_TEST_DSN=<local temporary PostgreSQL DSN> go test -run TestCollectionStatusRepository -count=1 ./internal/market_data/server/state
go run github.com/sqlc-dev/sqlc/cmd/sqlc@v1.31.1 generate
go test -count=1 ./cmd/gate/importboundary
go run ./cmd/gate/importboundary --repo-root=.
rg -n "collection_status|pgxpool" /home/x.go/internal/market_data/server/state /home/x.go/migrations
```

Results:

```text
ok   github.com/bytechainx/x.go/internal/market_data/server/state
ok   github.com/bytechainx/x.go/pkg/adapter/db/postgres
ok   github.com/bytechainx/x.go/internal/bootstrap
ok   github.com/bytechainx/x.go/cmd/gate/importboundary 0.021s
import-boundary-check pass findings=0 errors=0 warnings=0
```

`go run github.com/sqlc-dev/sqlc/cmd/sqlc@v1.31.1 generate` completed
successfully from the prior `/home/x.go` candidate. The collection-status
slice included x.go-owned sqlc configuration, query SQL, and generated query
code.
`collection_status` matches were found in the x.go-owned repository, tests,
query, generated, and migration files. The same search returned no `pgxpool`
matches under the collection-status state package, confirming that package
depends on `postgresx.DBTX` and `postgresx.TxRunner` instead of direct pool
ownership.

The prior x.go import-boundary gate included `B-POSTGRESX-001`, which fails
direct `pgxpool` imports under `internal/market_data/server/state/**` while
allowing the `pgx.TxOptions` and sqlc driver contracts required by `postgresx`
and generated code. This is the scoped CI-enforceable form of the goal's x.go
direct pool gate; the broad grep template would also catch existing x.go
adapter/infra pool ownership outside the collection-status integration slice.

The collection-status live PostgreSQL integration test passed with the local
temporary PostgreSQL DSN:

```text
ok   github.com/bytechainx/x.go/internal/market_data/server/state
```

Additional audit checks:

- `GOWORK=off go list -m` in `/home/postgresx` returned
  `github.com/bytechainx/postgresx`.
- `go env GOWORK` in `/home/x.go` returned `/home/go.work`.
- `go list -m github.com/bytechainx/postgresx` in `/home/x.go` returned
  `github.com/bytechainx/postgresx` with the workspace active.
- `GOWORK=off go list -m github.com/bytechainx/postgresx` in the prior
  `/home/x.go` candidate returned
  `github.com/bytechainx/postgresx v0.1.0 => /home/postgresx`.
- `go list -deps ./... | rg "github.com/bytechainx/x.go"` in
  `/home/postgresx` returned no matches.
- The prior `/home/x.go/go.mod` candidate required
  `github.com/bytechainx/postgresx v0.1.0` and used
  `replace github.com/bytechainx/postgresx => /home/postgresx`.
- The prior `/home/x.go/docs/releases/v0.3.0/manifest.yaml` candidate recorded
  `github.com/bytechainx/postgresx v0.1.0` under `dependency_versions`.

## Release DoD Audit

The implementation candidate has local evidence for README, CHANGELOG,
VERSION_MATRIX, integration docs, CI workflow files, and dirty=false migration
behavior. x.go dependency pinning, x.go release-manifest dependency entry, and
collection-status integration evidence are historical only for the current
checkout. The release DoD is not complete because the candidate has not been
authorized for commit, tag, push, or remote publication.

## Dependency Pins

Direct postgresx dependencies:

```text
github.com/golang-migrate/migrate/v4 v4.19.1
github.com/jackc/pgx/v5 v5.9.2
```

Version discovery references are recorded in `docs/VERSION_MATRIX.md`; complete
module checksums are recorded in `go.sum`.

## Release Gaps

- Git tag `v0.1.0` has not been created.
- The current `/home/postgresx` `HEAD` only tracks `.gitignore`, `LICENSE`,
  and `README.md`; the implementation, docs, CI, tests, and examples are still
  local working-tree changes. A release tag cut now would not represent this
  candidate unless the release commit/staging step is explicitly authorized
  first.
- Read-only remote checks found no remote `v0.1.0` tag and no GitHub release;
  remote publication is absent.
