# Agent Handoff Surface

This directory records the v0.1.0 delivery surfaces from `docs/goal.md` for
agent-readable review. Durable verification evidence remains in `docs/` and
runtime team state remains under `.omx/state`.
